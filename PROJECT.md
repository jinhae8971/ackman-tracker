# Ackman Tracker — 프로젝트 작업 보드

**상태**: 구현 완료, 통합 검증 통과 (Golden Data 7/7 PASS)
**최종 갱신**: 2026-08-02

이 파일이 작업 관리의 단일 진실 공급원이다. 각 작업의 상세 명세는 `tasks/` 에,
모듈 간 인터페이스 계약은 `specs/CONTRACTS.md` 에, 설계 근거는 `docs/architecture.md` 에 있다.

---

## 확정된 설계 결정

| # | 결정 | 근거 |
|---|---|---|
| D1 | 데이터 소스는 SEC 공시 전량 (13F + 13D/G + Form 4) | 무료·공식·법적 안전. 13D/A 182건이 13F 82건보다 촘촘 |
| D2 | 저장은 JSONL + Git (DB 없음) | 1만 행 규모. Git 이 감사 추적을 무료 제공 |
| D3 | 실행은 GitHub Actions 크론, 배포는 GitHub Pages | 서버 0대·시크릿 0개·비용 $0 |
| D4 | CUSIP→티커는 OpenFIGI v3 + 영구 캐시 | 무인증·정확·클래스 구분. CUSIP 은 불변이라 캐시 안전 |
| D5 | `value` 단위는 schemaVersion 으로 분기 정규화 | 구 스키마 천 달러 / X0202 달러. 미처리 시 1000배 왜곡 |
| D6 | 13F-HR/A 는 `amendmentType` 으로 대체/병합 분기 | NEW HOLDINGS 를 덮어쓰면 분기 포트폴리오 붕괴 |
| D7 | 대시보드는 단일 HTML + Chart.js (빌드 체인 없음) | 화면 5개 규모. 빌드 실패 모드 제거 |
| D8 | 매매 신호 생성하지 않음 | 13F 45일 지연. 신호로 쓰면 구조적 열위 |

---

## 작업 현황

| ID | 작업 | 담당 | 상태 | 산출물 |
|---|---|---|---|---|
| T0 | 아키텍처 설계 + 실측 검증 | 오케스트레이터 | ✅ 완료 | `docs/architecture.md` |
| T1 | 공유 계약 정의 | 오케스트레이터 | ✅ 완료 | `src/common/schema.py`, `specs/CONTRACTS.md` |
| T2 | Collector — 수집·파싱·매핑 | Agent A | ✅ 완료 | `src/collector/` (418 holdings / 735 filings / 52분기) |
| T3 | Analytics — diff·지표 | Agent B | ✅ 완료 | `src/analytics/` (75 tests, 453 events) |
| T4 | Pipeline — 오케스트레이션·워크플로우 | Agent C | ✅ 완료 | `src/pipeline/`, `.github/workflows/` (3종) |
| T5 | Dashboard — 정적 대시보드 | Agent D | ✅ 완료 | `dashboard/` (5화면, 1.07MB 데이터) |
| T6 | 통합 검증 — 실데이터 E2E | 오케스트레이터 | ✅ 완료 | 아래 §통합 검증 결과 |
| T7 | 13D 조기 감지 채널 확장 | 미배정 | 📋 백로그 | Phase 5 |
| T8 | 성과 귀속 (시장가 결합) | 미배정 | 📋 백로그 | Phase 6 |

범례: ✅ 완료 · 🔄 진행 · ⏳ 대기 · 📋 백로그 · ❌ 차단

---

## 병렬 작업 규율

각 에이전트는 자기 디렉터리 밖을 수정하지 않는다. `src/common/schema.py` 는 읽기 전용이며,
계약 변경이 필요하면 작업을 멈추고 오케스트레이터에게 보고한다.

```
Agent A → src/collector/          (네트워크 O)
Agent B → src/analytics/          (네트워크 X, 순수 함수)
Agent C → src/pipeline/, .github/ (조립만, 로직 중복 금지)
Agent D → dashboard/              (데이터 계약만 의존)
```

의존 방향은 단방향이다: `common ← collector ← analytics ← pipeline`, `common ← dashboard`.
역방향 import 는 금지한다.

---

## 알려진 함정 (구현 시 반드시 확인)

**단위 전환** — 실측 확인: `0001172661-22-002568`(2022-11) 합계 `7,877,045` = $7.88B(천 달러),
`0001172661-23-000673`(2023-02, X0202) 합계 `8,784,004,892` = $8.78B(달러).

**정정 병합** — 실측 확인: `0001172661-25-001497` 은 `amendmentType = NEW HOLDINGS`,
Hertz 1종목 $46,533,105 만 포함. 원본 `0001172661-25-001119` 는 10종목 $12,614,560,346.
올바른 병합 결과는 11종목 $12,661,093,451.

**클래스 분리** — Alphabet 은 `02079K107`(GOOG, Cl C) 과 `02079K305`(GOOGL, Cl A) 이 별개 행.
CUSIP 단독을 키로 쓰면 안 되고 `(cusip, title_of_class)` 를 써야 한다.

**주식 분할** — 무보정 시 `ADD` 오탐의 최대 원인. diff 이전에 반드시 보정.

**구조적 한계** — 13F 는 미국 상장 롱 포지션만 담는다. 애크먼의 스왑·CDS·헤지 포지션은
이 시스템에 원리적으로 보이지 않는다. 대시보드에 상시 고지할 것.

---

## 검증 기준선 (Golden Data)

통합 검증은 아래 실측값과 대조한다.

| 항목 | 기대값 |
|---|---|
| 2026-03-31 포지션 수 / 총액 | 11종목 / $13,714,299,861 |
| 2024-12-31 (병합 후) | 11종목 / $12,661,093,451 |
| 2022-09-30 (구 스키마) | 6종목 / $7,877,045,000 |
| 최대 보유 (2026Q1) | Brookfield Corp `11271J107` $2,415,946,008 / 59,697,208주 |
| Top-4 집중도 (2026Q1) | 약 66% |
| CUSIP 매핑 | `02079K107 → GOOG`, `02079K305 → GOOGL` |

---

## 통합 검증 결과 (T6 · 2026-08-02)

| # | 검증 항목 | 결과 |
|---|---|---|
| 1 | analytics 단위 테스트 | 75/75 PASS |
| 2 | pipeline 셀프테스트 + 게이트 테스트 | 39/39 · 10/10 PASS |
| 3 | events 재빌드 멱등성 | md5 `182d391a…` 동일 — PASS |
| 4 | 무결성 게이트 (실데이터 418행) | 위반 0건 — PASS |
| 5 | Golden Data 대조 | 7/7 PASS (3분기 합계 · 최대보유 · CUSIP 2건) |
| 6 | Top-4 집중도 (2026Q1) | 65.98% (기대 ~66%) — PASS |
| 7 | 대시보드 빌드 (정본) | 1,068,868 B / 52분기 — PASS |
| 8 | 워크플로우 YAML 3종 파싱·권한 | PASS |

**통합 중 수정한 결함 2건**

1. `dashboard/build.py` 말미 10행 유실 (샌드박스 마운트 동기화 지연) → 복원, 컴파일 통과.
2. `src/pipeline/run.py` 알림 범위 오류 — `notify.split_strong()` 에 전체 이력을 넘겨
   재빌드마다 과거 STRONG 135건이 전량 재알림되고 Issue 제목도
   `[STRONG] 2026-03-31 … 135건` 으로 잘못 표기됨. 최신 `report_date` 로 범위를
   한정하도록 수정 → `[STRONG] 2026-03-31 고확신 변화 4건` 으로 정상화.

**환경 제약 (코드 결함 아님)**: 샌드박스 마운트가 Windows 측 파일의 최신 바이트를
지연 반영한다. 검증은 Read 도구 기준을 정본으로 삼았다.

---

## 잔여 데이터 품질 한계 (문서화 완료, 차단 아님)

- 미해결 CUSIP 12건 (상장폐지 종목) — `unresolved: true` 로 캐시.
- CUSIP 변경쌍 `44267D107→44267T102`, `812215101→812215200` 은 EXIT+NEW 오탐 발생.
- `make_event_id` 에 `title_of_class` 미포함 — 충돌 시 빌드가 하드 실패로 방어 중.
- 2013Q2 이전 42건은 텍스트 전용 공시로 파싱 불가 — 시계열은 2013Q2 부터.
